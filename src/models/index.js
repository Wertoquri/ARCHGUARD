import * as workflowModel from './workflowModel.js';
import * as runModel from './runModel.js';

export const workflows = workflowModel;
export const runs = runModel;

export default { workflows, runs };
